---
import BaseLayout from "../layouts/BaseLayout.astro";
import site from "../data/site.json";
---
<BaseLayout title="Yhteys – AnomFIN" description="Ota yhteyttä AnomFINiin">
  <h1>Ota yhteyttä</h1>
  <p class="muted">Sähköposti: <a href={`mailto:${site.brand.emails.info}`}>{site.brand.emails.info}</a></p>
  <p class="muted">Tarjouspyynnöt: <a href={`mailto:${site.brand.emails.laskutus}`}>{site.brand.emails.laskutus}</a></p>
  <p class="muted">Rekry: <a href={`mailto:${site.brand.emails.rekry}`}>{site.brand.emails.rekry}</a></p>
</BaseLayout>

